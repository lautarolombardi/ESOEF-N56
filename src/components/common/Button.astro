---
interface Props {
  class?: string
}
const { class: className } = Astro.props
---

<button
  class={`py-2 px-4 bg-[var(--accent-color)] text-[var(--white)] hover:bg-white hover:text-[var(--accent-color)] transition-all duration-300 ${className}`}
>
  <slot />
</button>
